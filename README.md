
  # Structured Tools

  This is a code bundle for Structured Tools. The original project is available at https://www.figma.com/design/orrCpjGIXXp2NNPgRShgbe/Structured-Tools.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  